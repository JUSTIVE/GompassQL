[serve.static]
plugins = ["bun-plugin-tailwind"]
env = "BUN_PUBLIC_*"

[test]
# E2E specs live under `/e2e` and run under Playwright — keep them
# out of bun:test, which expects a different `test.describe` shape.
root = "src"
